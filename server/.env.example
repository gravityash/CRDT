# CollabSync Server Environment Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection String (Optional - falls back gracefully to In-Memory store if omitted or unreachable)
MONGO_URI=mongodb://127.0.0.1:27017/collabsync

# JWT Authentication Secret
JWT_SECRET=collabsync_super_secret_jwt_key_2026

# Client Origin (CORS)
CLIENT_URL=http://localhost:3000
